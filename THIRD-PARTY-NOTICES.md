# Third-party notices

This NuGet package redistributes official ClamAV command-line binaries from Cisco Talos. The runtime files are repackaged for NuGet without modifying executable or library bytes; development-only files may be omitted.

ClamAV is licensed under GPL-2.0-only. The GPL v2 text is included as `LICENSE` at the package root and as `COPYING.txt` in the ClamAV runtime directory. License notices for third-party components distributed with ClamAV are preserved in the runtime's `COPYING/` directory.

The runtime's `SOURCE.txt` records the exact upstream release, binary asset, and corresponding source archive used to create each package version.

ClamAV is a trademark of Cisco Systems, Inc. This package is not affiliated with or endorsed by Cisco Talos.
